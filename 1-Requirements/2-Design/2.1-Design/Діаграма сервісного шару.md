## Діаграма сервісного шару

```plantuml
@startuml
skinparam classAttributeIconSize 0

interface СервісКористувачів {
  + зареєструватиКористувача(даніКористувача: UserDTO): Користувач
  + оновитиРольКористувача(idКористувача: Long, роль: РольКористувача): Користувач
  + деактивуватиКористувача(idКористувача: Long): void
  + аутентифікувати(обліковіДані: LoginDTO): РезультатАутентифікації
}

interface СервісПостачальників {
  + шукатиПостачальників(критерії: КритеріїПошуку): List<Постачальник>
  + порівнятиПостачальників(idПостачальників: List<Long>): РезультатПорівняння
  + оновитиРейтингПостачальника(idПостачальника: Long, рейтинг: Double): void
  + отриматиІсторіюВзаємодії(idПостачальника: Long): List<ІсторіяВзаємодії>
}

interface СервісЗакупівель {
  + створитиЗаявкуНаЗакупівлю(даніЗаявки: PurchaseRequestDTO): ЗаявкаНаЗакупівлю
  + затвердитиЗаявку(idЗаявки: Long, idЗатверджувача: Long): void
  + автоматичнеЗатвердженняЗаявок(): List<ЗаявкаНаЗакупівлю>
  + отриматиСтатусЗаявки(idЗаявки: Long): СтатусЗаявки
}

interface СервісАналітики {
  + згенеруватиЗвіт(типЗвіту: ТипЗвіту, параметри: Map): Звіт
  + отриматиАналітикуПостачальника(idПостачальника: Long): АналітикаПостачальника
  + отриматиАналізЕкономіїВитрат(період: ДіапазонДат): АналізВитрат
}

interface СервісАудиту {
  + записатиДію(користувач: Користувач, дія: String, деталі: String): void
  + отриматиЖурналАудиту(користувач: Користувач): List<ЖурналАудиту>
  + отриматиПовнийЖурнал(): List<ЖурналАудиту>
}

class СервісКористувачівРеалізація {
  - репозиторійКористувачів: РепозиторійКористувачів
  - сервісШифрування: СервісШифрування
  - сервісАудиту: СервісАудиту
  + зареєструватиКористувача(даніКористувача: UserDTO): Користувач
  + оновитиРольКористувача(idКористувача: Long, роль: РольКористувача): Користувач
  + деактивуватиКористувача(idКористувача: Long): void
  + аутентифікувати(обліковіДані: LoginDTO): РезультатАутентифікації
}

class СервісПостачальниківРеалізація {
  - репозиторійПостачальників: РепозиторійПостачальників
  - пошуковийДвигун: ПошуковийДвигун
  - репозиторійІсторії: РепозиторійІсторії
  + шукатиПостачальників(критерії: КритеріїПошуку): List<Постачальник>
  + порівнятиПостачальників(idПостачальників: List<Long>): РезультатПорівняння
  + оновитиРейтингПостачальника(idПостачальника: Long, рейтинг: Double): void
  + отриматиІсторіюВзаємодії(idПостачальника: Long): List<ІсторіяВзаємодії>
}

class СервісЗакупівельРеалізація {
  - репозиторійЗакупівель: РепозиторійЗакупівель
  - процесЗатвердження: ПроцесЗатвердження
  - сервісАудиту: СервісАудиту
  + створитиЗаявкуНаЗакупівлю(даніЗаявки: PurchaseRequestDTO): ЗаявкаНаЗакупівлю
  + затвердитиЗаявку(idЗаявки: Long, idЗатверджувача: Long): void
  + автоматичнеЗатвердженняЗаявок(): List<ЗаявкаНаЗакупівлю>
  + отриматиСтатусЗаявки(idЗаявки: Long): СтатусЗаявки
}

class СервісАналітикиРеалізація {
  - репозиторійАналітики: РепозиторійАналітики
  - генераторЗвітів: ГенераторЗвітів
  + згенеруватиЗвіт(типЗвіту: ТипЗвіту, параметри: Map): Звіт
  + отриматиАналітикуПостачальника(idПостачальника: Long): АналітикаПостачальника
  + отриматиАналізЕкономіїВитрат(період: ДіапазонДат): АналізВитрат
}

class СервісАудитуРеалізація {
  - репозиторійАудиту: РепозиторійАудиту
  + записатиДію(користувач: Користувач, дія: String, деталі: String): void
  + отриматиЖурналАудиту(користувач: Користувач): List<ЖурналАудиту>
  + отриматиПовнийЖурнал(): List<ЖурналАудиту>
}

' Реалізації інтерфейсів
СервісКористувачівРеалізація .up.|> СервісКористувачів
СервісПостачальниківРеалізація .up.|> СервісПостачальників
СервісЗакупівельРеалізація .up.|> СервісЗакупівель
СервісАналітикиРеалізація .up.|> СервісАналітики
СервісАудитуРеалізація .up.|> СервісАудиту

@enduml
